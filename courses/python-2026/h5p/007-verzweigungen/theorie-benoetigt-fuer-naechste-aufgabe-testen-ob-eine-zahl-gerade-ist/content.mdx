Ein typischer Anwendungsfall in Python ist
die Überprüfung, ob eine Zahl gerade oder 
ungerade ist.

Dies macht man mit dem

```python

```

python
denn es gilt:

!!! info Definition
 Eine Zahl ist genau dann teilbar durch 2,  wenn sie ohne Rest durch 2 teilbar ist.
!!!

Da der

```python

%
```

python
kann man damit sehr gut überprüfen, ob eine Zahl durch
2 teilbar ist.

#### Beispiel:

```python

python
x = int(input())
if x % 2 == 0:
    print("Die Zahl ist gerade!")
if x % 2 != 0:
    print("Die Zahl ist nicht gerade")
```
