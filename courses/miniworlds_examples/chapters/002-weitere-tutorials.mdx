## Weitere Tutorials

### Kollisionen

<PythonQuestion
  identifier="rand-erkennen"
  title="Rand erkennen und abprallen"
/>

<PythonQuestion
  identifier="actors-zueinander"
  title="Kollisionen zwischen Figuren"
/>

Du lernst, wie Figuren mit dem Rand der Welt und mit anderen Figuren kollidieren.

#### Rand erkennen

```python
@ball.register
def act(self):
    self.move()
    borders = self.detect_borders()
    if borders:
        self.bounce_from_border(borders)
```

#### Abstand messen

```python
abstand = spieler.get_distance_to(ziel)
```

#### Berührung erkennen

```python
@ball.register
def act(self):
    if self.detect(ziel):
        print("Berührung!")
```

### Ereignisse

<PythonQuestion
  identifier="tastatur-events"
  title="Tastaturereignisse verwenden"
/>

<PythonQuestion
  identifier="maus-events"
  title="Mausereignisse verwenden"
/>

Du lernst, wie dein Programm auf Tastatur- und Mausereignisse reagiert.

#### Tastaturereignisse

```python
@player.register
def on_key_pressed_right(self):
    self.x = self.x + 10

@player.register
def on_key_pressed_a(self):
    self.turn_left(30)
```

#### Mausereignisse

```python
@world.register
def on_mouse_left(self):
    kreis = miniworlds.Circle((self.mouse_x, self.mouse_y), 20)
    kreis.color = (60, 120, 220)
```

### Animationen und Timer

<PythonQuestion
  identifier="animationen"
  title="Animationen verwenden"
/>

<PythonQuestion
  identifier="timer"
  title="Timer verwenden"
/>

Du lernst, wie du einfache Animationen und zeitgesteuerte Abläufe einsetzt.

#### Animationen

```python
knight = miniworlds.Actor((100, 100))
knight.add_costume("images/knight.png")
knight.add_costume("images/knight2.png")
knight.costume.animate()
```

#### Timer

```python
@world.register
def on_setup(self):
    self.set_timer("my_timer", 3)

@world.register
def on_timer_my_timer(self):
    print("3 Sekunden vorbei!")
```

### Gekachelte Welten

<PythonQuestion
  identifier="tiled-worlds"
  title="Gekachelte Welten aus Listen erstellen"
/>

<PythonQuestion
  identifier="kamera"
  title="Die Kamera verwenden"
/>

<PythonQuestion
  identifier="tiled-worlds-rpg"
  title="Einfache RPG-Welten erstellen"
/>

Du lernst, wie du gekachelte Welten aus Listen aufbaust und eine Kamera verwendest.

#### Level aus einer Liste erstellen

```python
level = [
    "##########",
    "#S.......#",
    "#..###...#",
    "#....#..Z#",
    "##########",
]

world = miniworlds.TiledWorld(10, 6)

for y, zeile in enumerate(level):
    for x, zeichen in enumerate(zeile):
        position = (x, y)
        if zeichen == "#":
            wand = miniworlds.Actor(position)
            wand.is_blocking = True
```

#### Kamera einer Figur folgen lassen

```python
world.camera.from_actor(spieler)
```
