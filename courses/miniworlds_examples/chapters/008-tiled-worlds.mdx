## Tiled Worlds

<PythonQuestion
  identifier="tiled-worlds"
/>

<PythonQuestion
  identifier="kamera"
/>

<PythonQuestion
  identifier="tiled-worlds-rpg"
/>

### Das Ziel

Du lernst, Worlds aus einer Liste zu erstellen.

### Level aus Liste erstellen

```python
level = [
    "##########",
    "#S.......#",
    "#..###...#",
    "#....#..Z#",
    "##########",
]

world = miniworlds.TiledWorld(10, 6)

for y, zeile in enumerate(level):
    for x, zeichen in enumerate(zeile):
        position = (x, y)
        if zeichen == "#":
            wand = miniworlds.Actor(position)
            wand.is_blocking = True
```

### Kamera folgen lassen

```python
world.camera.from_actor(spieler)
```
